# Web Server -  Python

This is a web server in Python with Flask


## Install
```
pip install flask
```

### ENV
create a virtual environment to run the application

Example:
```
py -3 -m venv env
```
